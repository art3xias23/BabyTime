<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BabyTime.Views.TodayPage"
             Title="Today"
             Shell.NavBarIsVisible="True">

    <Grid RowDefinitions="Auto,Auto,*,Auto" Padding="20,10,20,0">

        <Label Grid.Row="0"
               x:Name="DateLabel"
               FontSize="32"
               FontAttributes="Bold"
               HorizontalOptions="Center"
               Margin="0,10,0,0" />

        <Label Grid.Row="1"
               x:Name="DayLabel"
               FontSize="24"
               HorizontalOptions="Center"
               TextColor="Gray"
               Margin="0,0,0,20" />

        <CollectionView Grid.Row="2"
                        x:Name="ActivitiesCollectionView"
                        VerticalOptions="FillAndExpand"
                        Margin="0,0,0,10">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,5"
                            Padding="10,8"
                            BackgroundColor="White"
                            Stroke="#E0E0E0"
                            StrokeThickness="1">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="10"/>
                        </Border.StrokeShape>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <HorizontalStackLayout Grid.Column="0" Spacing="5">
                                <Label FontSize="16"
                                       VerticalOptions="Center">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="{Binding TimeString}"
                                                  FontAttributes="Bold"
                                                  TextColor="#007AFF"
                                                  FontSize="16" />
                                            <Span Text=" - "
                                                  TextColor="Gray"
                                                  FontSize="16" />
                                            <Span Text="{Binding EndTimeString}"
                                                  FontAttributes="Bold"
                                                  TextColor="#007AFF"
                                                  FontSize="16" />
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                                <Label Text="-"
                                       FontSize="16"
                                       VerticalOptions="Center"
                                       TextColor="Gray" />
                                <Label Text="{Binding ActivityType}"
                                       FontSize="16"
                                       VerticalOptions="Center"
                                       TextColor="Gray" />
                            </HorizontalStackLayout>

                            <Button Grid.Column="1"
                                    Text="Edit"
                                    BackgroundColor="#4CAF50"
                                    TextColor="White"
                                    FontSize="12"
                                    Padding="6,4"
                                    CornerRadius="5"
                                    Clicked="OnFinishClicked"
                                    CommandParameter="{Binding}"
                                    VerticalOptions="Center" />

                            <Button Grid.Column="2"
                                    Text="Delete"
                                    BackgroundColor="#FF3B30"
                                    TextColor="White"
                                    FontSize="12"
                                    Padding="6,4"
                                    Margin="3,0,0,0"
                                    CornerRadius="5"
                                    Clicked="OnDeleteClicked"
                                    CommandParameter="{Binding}"
                                    VerticalOptions="Center" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center"
                                     VerticalOptions="Center"
                                     Margin="0,50">
                    <Label Text="No activities yet"
                           FontSize="24"
                           TextColor="Gray"
                           HorizontalOptions="Center" />
                    <Label Text="Add your first activity from the Input tab"
                           FontSize="18"
                           TextColor="LightGray"
                           HorizontalOptions="Center"
                           Margin="0,10,0,0" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>
        </CollectionView>

        <Border Grid.Row="3"
                x:Name="SuccessBorder"
                BackgroundColor="#4CAF50"
                Padding="15,10"
                IsVisible="False"
                HorizontalOptions="Center"
                VerticalOptions="Start"
                Margin="0,10,0,10">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="5"/>
            </Border.StrokeShape>
            <Label Text="Deleted Successfully!"
                   TextColor="White"
                   FontSize="20"
                   FontAttributes="Bold" />
        </Border>

    </Grid>

</ContentPage>